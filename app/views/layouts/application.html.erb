<!DOCTYPE html>
<html>
	<head>
    		<title><%= title %></title>
    		<%= csrf_meta_tag %>
		
  	</head>
  	<body>
  		<%= render 'layouts/stylesheets' %>
    		
    	
			
			<section class="round">
				<%= yield %>
			</section>
			
		
			
			<%= debug(params) if Rails.env.development? %>
		</div>
  	</body>
</html>
